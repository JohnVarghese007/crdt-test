# Root env file for docker compose
# Copy to .env and fill in real values.

MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/crdt-test?retryWrites=true&w=majority&appName=PapyrusDB
JWT_SECRET=change-this-in-prod
